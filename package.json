{
  "name": "voice-chat-roulette",
  "version": "1.0.0",
  "description": "Голосовая чат-рулетка с PeerJS",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^4.18.2",
    "peer": "^0.6.1"
  },
  "engines": {
    "node": ">=18.x"
  }
}
